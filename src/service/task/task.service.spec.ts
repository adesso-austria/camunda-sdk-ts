import 'jasmine';

describe('Task Service', () => {
  describe('Factory', () => {
    xit('should return a task service', () => {
      // Given
      // const taskService = taskServiceFactory(mockRestClient as RestClient);

      // Then
      // expect(taskService).toBeTruthy();
    });
  });
});
